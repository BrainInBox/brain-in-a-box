# Lessons — captured corrections

Auto-filled by `correction-detector.py` (UserPromptSubmit hook). Every correction is appended immediately, under today's header.

Format:
```
## YYYY-MM-DD
- **[short context]** → rule: [what to do] (when: [trigger condition])
```

---
